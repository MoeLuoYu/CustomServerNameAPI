# 配置文件版本，请勿修改
version: 3.0
# 定义的服务器名称
server-name:
  # 服务器名称
  name: "Default Server Name"
  # 服务器别名
  alias: "Alias"
# 是否自动生成别名 注意：上方服务器名称为中文或其他非ASCII字符时，此选项无效
autogenerated: true
# 是否持久化占位符
# 默认值为 false，占位符将在插件卸载后被移除
# 如果为 true，占位符将在插件卸载后保留
persist: false